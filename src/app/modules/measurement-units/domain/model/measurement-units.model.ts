export interface UnidadesMedida {
  id: string;
  abreviacion: string;
  nombre: string;
  tipo_medida: string;
}

export interface MeasurementUnits {
  id: string;
  abbreviation: string;
  name: string;
  measurementType: string;
}
