export interface UnidadesMedida {
  id: number;
  abreviacion: string;
  nombre: string;
  tipo_medida: string;
}

export interface MeasurementUnits {
  id: number;
  abbreviation: string;
  name: string;
  measurementType: string;
}
